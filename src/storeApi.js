// import { useStore } from "./store";

// export const useStoreMethods = () => {
//   const { state, dispatch } = useStore();
//   return {
//     dailyCurationTopics: state.dailyCurationTopics,
//     dailyCurationTopic: state.dailyCurationTopic,
//     findRandomTopic: () => dispatch({ type: "increment", message: "Incremented" }),
//   };
// };
